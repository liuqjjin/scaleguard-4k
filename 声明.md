# ScaleGuard-4K 展示与证据声明

最后更新：2026-08-27

## 适用范围

本仓库同时承担两种用途：

1. 展示 ScaleGuard-4K 的研究问题、系统架构、算法流程、实验设计与预期公开形态；
2. 保存当前已经实现并可由代码、测试、运行清单或外部实验产物核验的工程事实。

为了避免把展示内容误读为实验事实，本文件统一说明两者的边界。README、架构图、实验表和发布材料中的数值，只有在存在下述真实证据时才能解释为实测结果：授权数据集清单、不可变输入与参考图哈希、非 mock GPU 运行清单、原始日志、环境与权重收据、指标收据及配对汇总。

## 当前已经验证的内容

截至本声明日期，仓库已经完成：

- 严格配置、CLI、倍率策略与显式尺度状态机；
- 恢复阶段、常驻生成会话及 `continue / stop / rollback` 的 CPU/mock 契约；
- 同尺寸质量增益、RGB NRMSE、Gradient MAE 与可选观测一致性实现；
- 五类前向退化模型：重采样、高斯 PSF、JPEG、Poisson–Gaussian 噪声和均匀雾；
- A-only、B-only、AB-fixed、ScaleGuard 四组实验编排与评估工具；
- 双 GPU 环境检查、部署、权重物化、运行、诊断与证据收集入口；
- 上游、补丁、依赖、模型修订和已知权重哈希的锁定；
- 1015 项 CPU、单元、契约、集成与评估测试；
- Python 3.10–3.14 CI、Ruff、格式检查、strict mypy 与 CodeQL。

这些内容证明软件和实验契约已经准备完成，不等同于证明真实模型的画质、速度、显存占用或研究结论。

## 展示性 GPU 内容

README 中的 GPU 指标表属于展示性研究目标和验收门槛，不是当前实测结果。具体包括但不限于：

- DIV4K-50的50组图像、4×四组200次推理、16×两组100次推理和总计300次推理的实验设置；
- 4×表中的PSNR、SSIM、LPIPS、MUSIQ、CLIPIQA、RGB NRMSE、Gradient MAE和43/50目标达到数；
- RGB跨尺度误差中位数下降27.5%及21.0%–34.0%的95%置信区间；
- Gradient MAE中位数下降22.0%及15.0%–29.0%的95%置信区间；
- PSNR差异−0.05 dB、LPIPS差异+0.003及对应置信区间；
- 16×表中的43/50首次接受、41/50达到4096×4096、最终尺度分布和五项条件画质指标；
- 74.6秒会话初始化、168.2秒首次4×、151.7秒第二次4×、286秒4×端到端、449秒16×端到端及其P95耗时；
- 21.8 GiB与22.6 GiB双卡主机级峰值显存；
- 任何尚未由真实运行产物支持的数据集名称、样本量、推理次数、4K 达标比例、停止率、回退率、时延、吞吐和显存数字；
- 任何用于说明未来结果表、Demo、消融图、失败案例或发布页面外观的情景化数值。

这些内容用于展示完整实验完成后应如何组织结果，不应作为论文结论、模型性能、求职经历中的实测数据或对外基准引用。

## 当前没有伪造或替代的证据

本仓库不会为展示目的生成或提交下列伪造材料：

- GPU 训练或推理日志；
- 数据集、校准集或评估集 manifest；
- 模型训练记录、checkpoint 变化或优化器状态；
- 真实实验运行清单与 GPU 采样；
- 阈值标定 receipt、指标 receipt 或消融 suite receipt；
- 冒充真实模型输出的生成图、结果 CSV、性能曲线或失败案例。

若某类材料不存在，展示页面只能说明目标结构，不能使用合成文件代替真实证据。

## 模型训练边界

ScaleGuard-4K 当前是推理、控制与评估项目。运行时加载预训练基础模型、恢复模型与已经发布的 LoRA/适配权重；仓库没有完成或宣称完成基础模型训练、SR LoRA 训练、VAE LoRA 训练、VLM 后训练、SFT、DPO、RLHF 或 GRPO。

因此，“GPU 实验”在本项目中主要指真实模型复现、端到端推理、阈值标定、四组消融、16×研究实验、系统性能测量和失败分析，不应改写为“完成了模型训练”。

## 当前证据等级

当前最高支持等级为 `STATIC_READY`：

- 代码、CPU/mock流程、配置、测试、部署入口和证据工具已经完成；
- 尚无经过审阅并保留的真实 GPU 组件输出；
- 尚无真实恢复到逐尺度生成的端到端运行证据；
- 尚无绑定真实运行配置的阈值标定收据；
- 尚无授权数据上的完整配对实验与研究汇总；
- 当前四组 runner 只覆盖单次 4×跃迁，16×两步配对协议仍需实现和运行。

项目证据等级依次为：

```text
STATIC_READY
COMPONENT_REPRODUCED
AB_INTEGRATED
SCALEGUARD_VALIDATED
RESEARCH_EVALUATED
```

只有在对应的不可变证据集合经过验证后，才允许提升等级。详细规则见 [项目证据状态](docs/results/STATUS.md) 和 [复现协议](docs/reproduction.md)。

## 真实实验后的更新规则

完成 GPU 实验后，应由工具直接从保留的运行证据生成结果，并在同一次提交中：

1. 固定数据集来源、许可、划分、输入/参考配对与 SHA-256；
2. 保留成功和失败的非 mock manifests、原始日志与 GPU 采样；
3. 记录环境、提交、权重、模型、seed、预处理和指标身份；
4. 在独立校准集上生成并验证门限收据；
5. 运行完整配对实验，不插补缺失组；
6. 生成 PSNR、SSIM、LPIPS、MUSIQ、CLIPIQA、跨尺度一致性和系统指标汇总；
7. 用真实统计量替换展示性数字，并同步更新 README、状态、限制和结果文档。

在上述步骤完成之前，本文件是仓库展示性数值的统一解释入口；[项目证据状态](docs/results/STATUS.md)仍是完成等级的权威记录。
